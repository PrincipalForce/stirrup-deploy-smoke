# stirrup-deploy-smoke

Smoke-test Stirrup project deployed to Launchmatic so the Workflows
tab, lm workflows CLI, and agent tools have something real to talk to.

Workflows: `hello-world` (pure transform), `echo-http` (POSTs to httpbin).
